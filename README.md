# PHP CRUD with XAMPP

This is a simple **CRUD (Create, Read, Update, Delete)** project built with **PHP**, using **XAMPP** (Apache + MySQL) as the server environment.

---

## Features

- User registration and login system
- Create, Read, Update, Delete operations on users or data
- Secure password hashing with `password_hash()`
- Session-based authentication
- MySQL database integration

---

## Requirements

- [XAMPP](https://www.apachefriends.org/) (Apache + MySQL)
- PHP 7.4+ (included in XAMPP)
- Web browser (Chrome, Firefox, etc.)

---

## Installation

1. Install XAMPP and start **Apache** and **MySQL**.
2. Copy this project folder into the `htdocs` directory of XAMPP:
